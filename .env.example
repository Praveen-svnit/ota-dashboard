# Copy this to .env and fill in values

# Absolute path to the SQLite database file on the server
# Default (if not set): ../ota-inv-db/data/ota.db  (relative to project root)
OTA_DB_PATH=/home/youruser/ota-inv-db/data/ota.db

# Next.js port (default 3000)
PORT=3000
